fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
jinja2==3.1.5
PyMuPDF==1.25.3
openpyxl==3.1.5
pdfplumber==0.11.9
# anthropic is optional - only needed for improved TOC page detection
# Install separately if desired: pip install anthropic==0.43.0
# docling is optional - for enhanced AI-powered table extraction
# Install separately if desired: pip install docling
